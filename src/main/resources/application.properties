spring.datasource.url=${DB_URL:jdbc:mysql://${MYSQLHOST:${MYSQLPRIVATEHOST:localhost}}:${MYSQLPORT:3306}/${MYSQLDATABASE:gamascape}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:${MYSQLUSER:root}}
spring.datasource.password=${DB_PASSWORD:${MYSQLPASSWORD:}}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false


jwt.secret=${JWT_SECRET:gamascape_super_secret_key_change_in_production_256bits_minimum}
jwt.expiration=${JWT_EXPIRATION:86400000}

server.port=${PORT:8080}
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
file.upload-dir=./uploads

# Mail Settings
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:gamascape@gmail.com}
spring.mail.password=${MAIL_PASSWORD:fqjkbnnbzebditza}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# 2026 Performance Boost: Virtual Threads
spring.threads.virtual.enabled=true

# Google Auth
google.client-id=${GOOGLE_CLIENT_ID:671393948918-u1uqlnlsqf71796vatcn96ll6o4r5lb2.apps.googleusercontent.com}
google.client-secret=${GOOGLE_CLIENT_SECRET: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}

# CORS and Cookie Security Settings for Production
allowed.origins=${ALLOWED_ORIGINS:http://localhost:4200,http://localhost:4205,https://gamascape.com,https://www.gamascape.com}
cookie.secure=${COOKIE_SECURE:false}
cookie.samesite=${COOKIE_SAMESITE:Lax}